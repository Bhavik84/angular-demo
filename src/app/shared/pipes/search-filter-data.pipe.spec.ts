import { SearchFilterDataPipe } from './search-filter-data.pipe';

describe('SearchFilterDataPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterDataPipe();
    expect(pipe).toBeTruthy();
  });
});
